const { createContext } = require("react");

export const AuthContext = createContext()

const AuthContextProivder=()=>{
    return<>
        <AuthContext.Provider>

        </AuthContext.Provider>
    </>
}

const ToggleAuth=()=>{
    
} 